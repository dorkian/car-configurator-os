Continue Car Configurator OS.

Goal:
Build the first usable 2D configurator UI.

Tasks:
1. Build a landing page that explains the project professionally.
2. Build a configurator page with:
   - image preview area
   - trim selector
   - color selector
   - wheel selector
   - interior selector
   - package selector
   - sticky build summary
3. Use local images with graceful placeholders if assets are missing.
4. Keep the UI premium, automotive, and recruiter-friendly.
5. Make the page responsive for mobile and desktop.
6. Add basic loading, empty, and error states.
7. Update README feature list and screenshots section placeholders.
8. Return a summary of completed UI slices and remaining gaps.